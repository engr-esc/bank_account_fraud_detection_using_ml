# Raw Data

Place original downloaded files here only if your submission rules allow it.

Expected files, if manually downloaded:
- Base.csv
- Variant II.csv

The preferred approach is to let the notebook download the dataset using `kagglehub`.
