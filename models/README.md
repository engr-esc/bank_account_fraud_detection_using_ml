# Models

This folder is reserved for saved model artifacts, such as the final champion pipeline.

The notebook saves model objects when the model implementation section is executed. Model files are ignored by default in `.gitignore` because they can be large. Include only final model artifacts if required by your capstone submission.

Recommended final artifact name:
- `final_model.pkl` or `champion_logistic_regression.joblib`
